apiVersion: v2
name: nginx
description: Repo-owned wrapper chart for nginx on the cluster.
type: application
version: 0.1.0
dependencies:
  - name: nginx
    alias: upstream
    version: ">=0.0.0"
    repository: oci://registry-1.docker.io/bitnamicharts
