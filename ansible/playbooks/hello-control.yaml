# name: hello-control
# description: Ping the control node group.
# target: control
# requires_approval: true
# tags:
#   - connectivity
#   - control

- hosts: control
  become: yes
  tasks:
    - name: Ping
      ping:
